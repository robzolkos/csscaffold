# Example Output

These are the CSS files generated by `csscaffold`.

Use these as a reference to see:
- What file structure gets created
- What each file contains
- The organization and comments in each file
- Examples of how to fill in each template

To create these files in your Rails app, run:

```bash
csscaffold
```

From the root of your Rails project.
